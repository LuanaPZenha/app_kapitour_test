/** Escala de 8pt — base para ritmo visual consistente em todo o app. */
export const spacing = {
  xxs: 4,
  xs: 8,
  sm: 12,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 40,
};

export const radius = {
  sm: 8,
  md: 12,
  lg: 16,
  pill: 24,
  round: 999,
};

export const layout = {
  minTouchTarget: 44,
  maxFormWidth: 400,
  contentPadding: spacing.md,
};
